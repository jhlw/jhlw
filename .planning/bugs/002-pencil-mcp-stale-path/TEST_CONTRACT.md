# Test Contract

- Old configured path must not exist.
- New configured path must exist.
- Direct JSON-RPC `initialize` request must return `serverInfo.name = pencil`.
- `codex mcp list` must show the `0.6.70` executable and exit 0.
